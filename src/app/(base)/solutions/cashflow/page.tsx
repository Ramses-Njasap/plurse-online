// src/app/solutions/cashflow/page.tsx
import CashFlowPage from "@/components/solutions/cashflow";

export const metadata = {
    title: "Cash Flow Clarity — Plurse",
    description: "From first purchase to final payment — every franc accounted for. Track income, expenses, credit and margins with Plurse.",
};

const CashFlow = () => {
    return <CashFlowPage />;
};

export default CashFlow;