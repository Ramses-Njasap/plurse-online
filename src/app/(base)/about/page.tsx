// src/app/about/page.tsx
import AboutPage from "@/components/about";

export const metadata = {
    title: "About — Plurse",
    description: "Building the operating system for African business. Software, intelligence, masterclasses and community — all in one platform.",
};

const Page = () => {
    return <AboutPage />;
};

export default Page;