# Plurse Access Key - Matrix & Combination Graph

---

## 1. Complete Combination Matrix Reference

| ID | `is_standalone` | `from_company` | `channel_partner_id` | `business_id` | Architectural Meaning & UI Context |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **#1** | `true` | `true` | `NOT NULL (Creator)` | `NULL` | **Standalone Inventory:** Floating token sitting in a user's wallet. Ready to be linked. |
| **#2** | `false` | `true` | `NULL` | `NOT NULL (Biz)` | **Self-Owned Business:** Key actively running the user's personal business. |
| **#3** | `false` | `false` | `NOT NULL (Partner)` | `NOT NULL (Client)` | **Partner Distributed:** Active billing client generated by a Channel Partner. |
| **#4** | `false` | `true` | `NULL` | `NOT NULL (Biz)` | **Direct Company Key:** Global key created by system admins, bypasses partners. |
| **#5** | `false` | `true` | `NOT NULL (Partner)` | `NOT NULL (BIZ)` | **Historical Unlinked Key:** Client unlinked a partner key. Retained for partner audit tracking. |

---

## 2. Dynamic Matrix Structural Graph

```mermaid
graph TD
    %% Node Definitions matching the Matrix IDs
    M1["[#1] Standalone Inventory <br/> is_standalone: true <br/> from_company: true <br/> partner_id: NOT NULL <br/> biz_id: NULL"]
    
    M2["[#2] Self-Owned Business <br/> is_standalone: false <br/> from_company: true <br/> partner_id: NULL <br/> biz_id: NOT NULL"]
    
    M3["[#3] Partner Distributed <br/> is_standalone: false <br/> from_company: false <br/> partner_id: NOT NULL <br/> biz_id: NOT NULL"]
    
    M4["[#4] Direct Company Key <br/> is_standalone: false <br/> from_company: true <br/> partner_id: NULL <br/> biz_id: NOT NULL"]
    
    M5["[#5] Historical Unlinked <br/> is_standalone: false <br/> from_company: true <br/> partner_id: NOT NULL <br/> biz_id: NOT NULL (BIZ)"]

    %% Direct Structural Pathways & Transitions
    M1 -->|Link to Self Wizard Toggle| M2
    M1 -->|Link to External Client| M3
    
    M3 -->|Client Breaks Contract / Unlinks| M5
    M5 -->|Reassign Key to New Client| M3
    
    %% Admin Channel Injection
    AdminMint((Admin Dashboard)) -->|Direct System Assignment| M4

    %% Styling Theme for Column States
    style M1 fill:#dbeafe,stroke:#2563eb,stroke-width:2px
    style M2 fill:#dcfce7,stroke:#16a34a,stroke-width:2px
    style M3 fill:#fef3c7,stroke:#d97706,stroke-width:2px
    style M4 fill:#f3f4f6,stroke:#4b5563,stroke-width:2px
    style M5 fill:#fee2e2,stroke:#dc2626,stroke-width:2px